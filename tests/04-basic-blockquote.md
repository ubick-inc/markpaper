# 引用テスト

## 単一行引用

> これは重要な引用文です。

## 複数行引用

> このセクションでは引用文とコードブロックが混在しています。
>
> すべてが適切に表示されるかをテストします。

## コードとの組み合わせ

> 設定例:

```javascript
const config = {
  pageBreak: {
    beforeH1: true,
    beforeH2: false
  }
};
```

> 上記の設定により、章レベルでの改ページ制御が可能になります。
